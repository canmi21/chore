[package]
name = "chore-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "chore"
path = "src/main.rs"

[lib]

[dependencies]
clap = { version = "4", features = ["derive"] }
glob = "0.3"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
walkdir = "2"
